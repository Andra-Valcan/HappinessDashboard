package com.example.PCandra.repository;

import com.example.PCandra.entity.UserChoise;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserChoiseRepository extends JpaRepository<UserChoise, Long> {
}
